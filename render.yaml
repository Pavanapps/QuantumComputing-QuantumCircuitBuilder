services:
  - type: web
    name: quantum-circuit-builder-lite
    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      gunicorn app:app

    plan: free